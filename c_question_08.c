// program to solve given equation (3*x)/(y-z+k), where x=2,y=3,z=3,k=1
#include <stdio.h>
int main()
{
    int x=2,
    y=3,
    z=3,
    k=1;
    float ans=((3*x)/(y-z+k));
    printf("\n the answer is %f",ans);
    
        return 0;
}